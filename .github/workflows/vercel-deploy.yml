name: Deploy frontend-desktop to Vercel

on:
  push:
    paths:
      - 'frontend-desktop/**'

jobs:
  vercel-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: |
          cd frontend-desktop
          if [ -f package-lock.json ] ; then npm ci ; else npm install ; fi

      - name: Build project
        run: |
          cd frontend-desktop
          npm run build

      - name: Install Vercel CLI
        run: |
          cd frontend-desktop
          npm install -D vercel

      - name: Deploy to Vercel (prebuilt)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          cd frontend-desktop
          npx vercel build
          npx vercel deploy --prebuilt --prod --token $VERCEL_TOKEN --confirm
